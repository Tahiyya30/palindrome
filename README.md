Palindrome Test

This program determines if a word is a palindrome or not.

How it works

1.) User is asked for a word.

2.) The word is then split into an array of letters, looped through in reverse and the reversed word is saved to a variable.

3.) If the variable equals the original word, the word inputted by the user is a palindrome.

Files included:

Javascript file

HTML file
